[package]
name = "ptyknot"
version = "0.1.0"
authors = ["Bart Massey <bart.massey@gmail.com>"]
publish = false
description = "Run a specified action as a new process with controlling pty."
repository = "http://gitlab.com/BartMassey/ptyknot"
readme = "README.md"
keywords = ["pty", "pseudotty", "process"]
license-file = "COPYING"

[lib]
name = "ptyknot"
path = "src/lib.rs"

[dependencies]
libc = "0.2.0"
